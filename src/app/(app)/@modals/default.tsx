/**
 * Neutral state of the `@modals` slot, rendered when the current URL
 * matches no intercepted route. Required by every Next.js parallel slot.
 */
export default function Default() {
  return null;
}
